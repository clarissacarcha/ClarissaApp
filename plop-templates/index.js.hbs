/**
 * @format
 * @flow
 */

import React from 'react';
import type {PropsType} from './types';
import {Container} from './Styled';

export const {{pascalCase name}} = (props: PropsType): React$Node => {
  return <Container></Container>;
};
