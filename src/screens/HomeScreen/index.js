/**
 * @format
 * @flow
 */

import React from 'react';
import type {PropsType} from './types';
import {Container, Title} from './Styled';

export const HomeScreen = (props: PropsType): React$Node => {
  return (
    <Container>
      <Title>Home Screen</Title>
    </Container>
  );
};
